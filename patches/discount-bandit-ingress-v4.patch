diff --git a/app/Classes/Crawler/ChromiumCrawler.php b/app/Classes/Crawler/ChromiumCrawler.php
index da96e3c..6acb26a 100644
--- a/app/Classes/Crawler/ChromiumCrawler.php
+++ b/app/Classes/Crawler/ChromiumCrawler.php
@@ -10,8 +10,8 @@ use HeadlessChromium\Exception\CommunicationException;
 use HeadlessChromium\Exception\NoResponseAvailable;
 use HeadlessChromium\Exception\OperationTimedOut;
 use HeadlessChromium\Page;
+use Illuminate\Support\Facades\File;
 use Illuminate\Support\Facades\Log;
-use Illuminate\Support\Facades\Process;
 use Illuminate\Support\Str;
 use InvalidArgumentException;

@@ -27,62 +27,78 @@ class ChromiumCrawler
         int $timeout_ms = 5000,
         string $page_event = Page::NETWORK_IDLE,
     ) {
-        // path to the file to store websocket's uri
-        $socketFile = '/tmp/chrome-php-demo-socket';
+        $keepAlive = filter_var(env('DISCOUNT_BANDIT_CHROMIUM_KEEP_ALIVE', false), FILTER_VALIDATE_BOOL);
+        $enableImages = filter_var(env('DISCOUNT_BANDIT_CHROMIUM_ENABLE_IMAGES', false), FILTER_VALIDATE_BOOL);
+        $userDataDir = $keepAlive
+            ? env('DISCOUNT_BANDIT_CHROMIUM_USER_DATA_DIR', '/tmp/discount-bandit-chromium-profile')
+            : sys_get_temp_dir().'/discount-bandit-chromium-'.getmypid().'-'.bin2hex(random_bytes(6));
+        $socketFile = env('DISCOUNT_BANDIT_CHROMIUM_SOCKET_FILE', '/tmp/discount-bandit-chromium-socket');

-        if (! file_exists($socketFile)) {
-            file_put_contents($socketFile, '');
-        }
-
-        $socket = file_get_contents($socketFile);
+        File::ensureDirectoryExists($userDataDir);

         $options = [
             //
             'connectionDelay' => 1.32,
-            'headless' => false,
+            'enableImages' => $enableImages,
+            'headless' => true,
             'noSandbox' => true,
             "headers" => $extra_headers,
+            'keepAlive' => $keepAlive,
+            'userDataDir' => $userDataDir,
             'customFlags' => [
                 '--lang=en-US',
                 '--disable-blink-features=AutomationControlled',
+                '--disable-background-networking',
                 '--deny-permission-prompts=true',
                 '--disable-blink-features=AutomationControlled',
                 '--disable-web-security',
+                '--disable-extensions',
                 '--disable-features=IsolateOrigins,site-per-process',
+                '--disable-sync',
                 '--disable-site-isolation-trials',
                 '--ignore-certificate-errors',
+                '--metrics-recording-only',
+                '--mute-audio',
                 '--no-first-run',
                 '--no-default-browser-check',
+                '--no-zygote',
                 '--no-sandbox',
                 '--test-type',
                 '--enable-features=NetworkService,NetworkServiceInProcess',
                 '--window-size=1920,1080',
             ],
             'disableNotifications' => true,
-            'keepAlive' => true,
             'ignoreHTTPSErrors' => true,
             'ignoreCertificateErrors' => true,
             'stealth' => true,
             'bypassCSP' => true,
         ];

-        try {
-            $browser = BrowserFactory::connectToBrowser($socket);
-        } catch (BrowserConnectionFailed|InvalidArgumentException $e) {
-            Log::info('New Chrome instance');
-            Log::info($e->getMessage());
-            $browserType = app()->isProduction() ? 'chromium' : null;
+        $browser = null;
+        $page = null;

-            // just in case there is a chromium process running on the server
-            Process::run("killall chromium");
+        if ($keepAlive && file_exists($socketFile)) {
+            try {
+                $browser = BrowserFactory::connectToBrowser((string) file_get_contents($socketFile));
+            } catch (BrowserConnectionFailed|InvalidArgumentException $e) {
+                Log::warning('Could not reconnect to existing Chrome instance; starting a new one.', [
+                    'message' => $e->getMessage(),
+                ]);
+            }
+        }
+
+        if ($browser === null) {
+            Log::debug('New Chrome instance', ['keep_alive' => $keepAlive]);
+            $browserType = app()->isProduction() ? 'chromium' : null;

-            // The browser was probably closed, start it again
             $browser_factory = new BrowserFactory($browserType);

             $browser = $browser_factory
                 ->createBrowser($options);
-            // save the uri to be able to connect again to the browser
-            file_put_contents($socketFile, $browser->getSocketUri(), LOCK_EX);
+
+            if ($keepAlive) {
+                file_put_contents($socketFile, $browser->getSocketUri(), LOCK_EX);
+            }
         }

         try {
@@ -101,16 +117,36 @@ class ChromiumCrawler
         } catch (NoResponseAvailable $e) {
             Log::error("No response available");
         } catch (OperationTimedOut $e) {
-            $this->dom = HTMLDocument::createFromString($page->getHtml(), LIBXML_NOERROR);
+            if ($page !== null) {
+                $this->dom = HTMLDocument::createFromString($page->getHtml(), LIBXML_NOERROR);
+            }
         } catch (Exception $exception) {
             Log::error("Crawling using chrome");
             Log::error($exception->getMessage());
+        } finally {
+            if ($page !== null) {
+                try {
+                    $page->close();
+                } catch (CommunicationException) {
+                    Log::error("Couldn't close the page");
+                } catch (Exception) {
+                    // The browser may already be gone after navigation errors.
+                }
+            }
+
+            if (! $keepAlive && $browser !== null) {
+                try {
+                    $browser->close();
+                } catch (Exception) {
+                    // The process may already have exited after a failed launch.
+                }
+
+                File::deleteDirectory($userDataDir);
+            }
         }

-        try {
-            $page?->close();
-        } catch (CommunicationException $e) {
-            Log::error("Couldn't close the page");
+        if (! isset($this->dom)) {
+            $this->dom = HTMLDocument::createFromString('', LIBXML_NOERROR);
         }
     }

diff --git a/app/Classes/CustomStoreTemplate.php b/app/Classes/CustomStoreTemplate.php
index 55c2278..4086090 100644
--- a/app/Classes/CustomStoreTemplate.php
+++ b/app/Classes/CustomStoreTemplate.php
@@ -113,7 +113,9 @@ class CustomStoreTemplate
                 user_agent: $this->user_agent,
             )->dom;
         }
-        $this->dom->saveHTMLFile('response.html');
+        if (filter_var(env('DISCOUNT_BANDIT_SAVE_CRAWL_RESPONSE', false), FILTER_VALIDATE_BOOL)) {
+            $this->dom->saveHTMLFile(storage_path('logs/response.html'));
+        }

         $this->schema = new SchemaParser($this->dom)->schema;

diff --git a/app/Classes/StoreTemplate.php b/app/Classes/StoreTemplate.php
index 1dd7829..b60eef0 100644
--- a/app/Classes/StoreTemplate.php
+++ b/app/Classes/StoreTemplate.php
@@ -102,7 +102,9 @@ abstract class StoreTemplate
                 user_agent: $this->user_agent,
             )->dom;
         }
-        $this->dom->saveHTMLFile('response.html');
+        if (filter_var(env('DISCOUNT_BANDIT_SAVE_CRAWL_RESPONSE', false), FILTER_VALIDATE_BOOL)) {
+            $this->dom->saveHTMLFile(storage_path('logs/response.html'));
+        }

         $this->schema = new SchemaParser($this->dom)->schema;
     }
diff --git a/app/Console/Commands/DiscountFillSupervisorWorkersCommand.php b/app/Console/Commands/DiscountFillSupervisorWorkersCommand.php
index b2cd79b..f00d85f 100644
--- a/app/Console/Commands/DiscountFillSupervisorWorkersCommand.php
+++ b/app/Console/Commands/DiscountFillSupervisorWorkersCommand.php
@@ -45,14 +45,16 @@ class DiscountFillSupervisorWorkersCommand extends Command
 [program:laravel-worker-{$store->id}]
 process_name=%(program_name)s_%(process_num)02d
 user=root
-command=php artisan queue:work --queue={$store->slug} --max-time=300 --sleep=1 --tries=1
+command=php artisan queue:work --queue={$store->slug} --max-time=180 --max-jobs=25 --memory=192 --timeout=60 --sleep=2 --tries=1
 autostart=true
 autorestart=true
 stopasgroup=true
 killasgroup=true
 numprocs=1
 redirect_stderr=true
-stdout_logfile=/var/log/worker.log
+stdout_logfile=/logs/worker-{$store->slug}.log
+stdout_logfile_maxbytes=5MB
+stdout_logfile_backups=2
 stopwaitsecs=300
                     ");

diff --git a/app/Filament/Pages/Auth/Login.php b/app/Filament/Pages/Auth/Login.php
index 7047037..5924f94 100644
--- a/app/Filament/Pages/Auth/Login.php
+++ b/app/Filament/Pages/Auth/Login.php
@@ -2,16 +2,44 @@

 namespace App\Filament\Pages\Auth;

+use App\Filament\Pages\Dashboard;
 use App\Models\User;
+use Filament\Facades\Filament;
+use Illuminate\Support\Facades\Log;

 class Login extends \Filament\Auth\Pages\Login
 {
     public function mount(): void
     {
         if (! User::exists()) {
-            redirect()->to('/register');
+            Log::info('Discount Bandit auth redirect', [
+                'page' => 'login',
+                'reason' => 'missing_user',
+                'target' => route('filament.admin.auth.register'),
+                'users_count' => User::count(),
+            ]);
+
+            $this->redirect(route('filament.admin.auth.register'), navigate: true);
+
+            return;
+        }
+
+        if (Filament::auth()->check()) {
+            $target = Dashboard::getUrl(panel: 'admin');
+
+            Log::info('Discount Bandit auth redirect', [
+                'page' => 'login',
+                'reason' => 'already_authenticated',
+                'target' => $target,
+                'users_count' => User::count(),
+                'authenticated' => true,
+            ]);
+
+            $this->redirect($target, navigate: true);
+
+            return;
         }

-        parent::mount(); // TODO: Change the autogenerated stub
+        parent::mount();
     }
 }
diff --git a/app/Filament/Pages/Auth/Register.php b/app/Filament/Pages/Auth/Register.php
index fc5cd0e..0fb84bc 100644
--- a/app/Filament/Pages/Auth/Register.php
+++ b/app/Filament/Pages/Auth/Register.php
@@ -6,6 +6,7 @@ use App\Enums\RoleEnum;
 use App\Models\User;
 use Filament\Actions\Action;
 use Filament\Forms\Components\Placeholder;
+use Illuminate\Support\Facades\Log;

 class Register extends \Filament\Auth\Pages\Register
 {
@@ -13,22 +14,54 @@ class Register extends \Filament\Auth\Pages\Register
     public function mount(): void
     {
         if (User::exists()) {
-            redirect()->to('/login');
+            Log::info('Discount Bandit auth redirect', [
+                'page' => 'register',
+                'reason' => 'user_exists',
+                'target' => route('filament.admin.auth.login'),
+                'users_count' => User::count(),
+            ]);
+
+            $this->redirect(route('filament.admin.auth.login'), navigate: true);
+
+            return;
         }

-        parent::mount(); // TODO: Change the autogenerated stub
+        parent::mount();
     }

     public function getRegisterFormAction(): Action
     {
         if (User::count() > 0) {
             return Action::make('redirect')
-                ->action(fn () => redirect()->to('/'));
+                ->action(function (): void {
+                    Log::info('Discount Bandit auth redirect', [
+                        'page' => 'register',
+                        'reason' => 'register_action_with_existing_user',
+                        'target' => route('filament.admin.auth.login'),
+                        'users_count' => User::count(),
+                    ]);
+
+                    $this->redirect(route('filament.admin.auth.login'), navigate: true);
+                });
         }

         return parent::getRegisterFormAction();
     }

+    protected function getRedirectUrl(): string
+    {
+        $target = route('filament.admin.auth.login');
+
+        Log::info('Discount Bandit auth redirect', [
+            'page' => 'register',
+            'reason' => 'register_success',
+            'target' => $target,
+            'users_count' => User::count(),
+        ]);
+
+        return $target;
+    }
+
     protected function mutateFormDataBeforeRegister(array $data): array
     {
         $data['role'] = RoleEnum::Admin->value;
diff --git a/app/Filament/Pages/Dashboard.php b/app/Filament/Pages/Dashboard.php
index 22206e7..9ad1516 100644
--- a/app/Filament/Pages/Dashboard.php
+++ b/app/Filament/Pages/Dashboard.php
@@ -8,6 +8,7 @@ use Filament\Schemas\Contracts\HasSchemas;

 class Dashboard extends \Filament\Pages\Dashboard implements HasSchemas
 {
+    protected static string $routePath = 'dashboard';

     public function getWidgets(): array
     {
diff --git a/app/Http/Middleware/TrustIngressPath.php b/app/Http/Middleware/TrustIngressPath.php
new file mode 100644
index 0000000..9a91e1f
--- /dev/null
+++ b/app/Http/Middleware/TrustIngressPath.php
@@ -0,0 +1,236 @@
+<?php
+
+namespace App\Http\Middleware;
+
+use App\Models\User;
+use Closure;
+use Illuminate\Http\Request;
+use Illuminate\Support\Facades\Log;
+use Illuminate\Support\Facades\URL;
+use Illuminate\Support\Str;
+use Symfony\Component\HttpFoundation\Response;
+
+class TrustIngressPath
+{
+    public function handle(Request $request, Closure $next): Response
+    {
+        $context = $this->resolveIngressContext($request);
+
+        if ($context !== null) {
+            $this->applyBaseUrl($context);
+        }
+
+        if ($context !== null && $this->shouldLogRequest($request)) {
+            Log::info('Discount Bandit ingress request', $this->buildLogContext($request, $context));
+        }
+
+        $response = $next($request);
+
+        if ($context !== null && $this->shouldLogRequest($request)) {
+            Log::info('Discount Bandit ingress response', array_merge(
+                $this->buildLogContext($request, $context),
+                [
+                    'status' => $response->getStatusCode(),
+                    'location' => $response->headers->get('Location'),
+                    'is_redirect' => $response->isRedirection(),
+                ]
+            ));
+        }
+
+        if ($context !== null) {
+            $this->rewriteIngressAwareResponse($response, $context);
+        }
+
+        return $response;
+    }
+
+    protected function resolveIngressContext(Request $request): ?array
+    {
+        $scheme = $request->headers->get('X-Forwarded-Proto') ?: $request->getScheme();
+        $host = $request->headers->get('X-Forwarded-Host') ?: $request->getHttpHost();
+        $ingressPath = trim((string) $request->headers->get('X-Ingress-Path', ''), " \t\n\r\0\x0B");
+
+        if ($host === '') {
+            return null;
+        }
+
+        $normalizedIngressPath = $ingressPath === ''
+            ? ''
+            : '/'.trim($ingressPath, '/');
+
+        return [
+            'base_url' => rtrim(sprintf('%s://%s%s', $scheme, $host, $normalizedIngressPath), '/'),
+            'host' => $host,
+            'ingress_path' => $normalizedIngressPath,
+            'scheme' => $scheme,
+        ];
+    }
+
+    protected function applyBaseUrl(array $context): void
+    {
+        $baseUrl = $context['base_url'];
+        $cookiePath = $context['ingress_path'] === ''
+            ? '/'
+            : $context['ingress_path'];
+
+        URL::forceRootUrl($baseUrl);
+        URL::forceScheme(parse_url($baseUrl, PHP_URL_SCHEME) ?: 'http');
+
+        config([
+            'app.url' => $baseUrl,
+            'app.asset_url' => $baseUrl,
+            'livewire.asset_url' => $baseUrl.'/livewire/livewire.js',
+            'filesystems.disks.public.url' => $baseUrl.'/storage',
+            'filesystems.disks.store.url' => $baseUrl.'/storage/store',
+            'session.path' => $cookiePath,
+            'session.secure' => $context['scheme'] === 'https',
+            'session.same_site' => $context['ingress_path'] === '' ? config('session.same_site') : 'none',
+            'session.partitioned' => $context['ingress_path'] !== '',
+        ]);
+    }
+
+    protected function shouldLogRequest(Request $request): bool
+    {
+        return $request->is('login')
+            || $request->is('register')
+            || $request->is('livewire/*')
+            || $request->is('dashboard*')
+            || $request->is('products*')
+            || $request->path() === '/';
+    }
+
+    protected function rewriteIngressAwareResponse(Response $response, array $context): void
+    {
+        $contentType = (string) $response->headers->get('Content-Type', '');
+
+        if (! Str::contains($contentType, ['text/html', 'application/json', 'application/vnd.livewire'])) {
+            return;
+        }
+
+        $content = $response->getContent();
+
+        if (! is_string($content) || $content === '') {
+            return;
+        }
+
+        $ingressPath = $context['ingress_path'];
+        $baseUrl = $context['base_url'];
+        $hostRoot = $context['scheme'].'://'.$context['host'];
+        $homeUrl = $baseUrl.'/dashboard';
+        $jsonEscapedBaseUrl = str_replace('/', '\/', $baseUrl);
+        $jsonEscapedHostRoot = str_replace('/', '\/', $hostRoot);
+        $jsonEscapedHomeUrl = str_replace('/', '\/', $homeUrl);
+        $jsonEscapedIngressPath = str_replace('/', '\/', $ingressPath);
+
+        if ($ingressPath === '') {
+            return;
+        }
+
+        $rewritten = str_replace(
+            [
+                $hostRoot.'/livewire/',
+                $hostRoot.'/storage/',
+                'http://localhost/storage/',
+                ' href="'.$hostRoot.'/"',
+                ' href="'.$hostRoot.'"',
+                " href='".$hostRoot."/'",
+                " href='".$hostRoot."'",
+                ' href="'.$baseUrl.'/"',
+                ' href="'.$baseUrl.'"',
+                " href='".$baseUrl."/'",
+                " href='".$baseUrl."'",
+                '"/livewire/',
+                '"/storage/',
+                "'/livewire/",
+                "'/storage/",
+                ' href="/"',
+                " href='/'",
+                ' src="/livewire/',
+                ' src="/storage/',
+                ' data-update-uri="/livewire/',
+                'href=\"'.$hostRoot.'\/\"',
+                'href=\"'.$jsonEscapedHostRoot.'\/\"',
+                'href=\"'.$hostRoot.'\"',
+                'href=\"'.$jsonEscapedHostRoot.'\"',
+                'href=\"'.$baseUrl.'\/\"',
+                'href=\"'.$jsonEscapedBaseUrl.'\/\"',
+                'href=\"'.$baseUrl.'\"',
+                'href=\"'.$jsonEscapedBaseUrl.'\"',
+                'href=\"\/\"',
+                'href=\"/\"',
+                'src=\"\/livewire\/',
+                'src=\"/livewire/',
+                'src=\"\/storage\/',
+                'src=\"/storage/',
+                'data-update-uri=\"\/livewire\/',
+                'data-update-uri=\"/livewire/',
+            ],
+            [
+                $baseUrl.'/livewire/',
+                $baseUrl.'/storage/',
+                $baseUrl.'/storage/',
+                ' href="'.$homeUrl.'"',
+                ' href="'.$homeUrl.'"',
+                " href='".$homeUrl."'",
+                " href='".$homeUrl."'",
+                ' href="'.$homeUrl.'"',
+                ' href="'.$homeUrl.'"',
+                " href='".$homeUrl."'",
+                " href='".$homeUrl."'",
+                '"'.$ingressPath.'/livewire/',
+                '"'.$ingressPath.'/storage/',
+                "'".$ingressPath.'/livewire/',
+                "'".$ingressPath.'/storage/',
+                ' href="'.$homeUrl.'"',
+                " href='".$homeUrl."'",
+                ' src="'.$ingressPath.'/livewire/',
+                ' src="'.$ingressPath.'/storage/',
+                ' data-update-uri="'.$ingressPath.'/livewire/',
+                'href=\"'.$jsonEscapedHomeUrl.'\"',
+                'href=\"'.$jsonEscapedHomeUrl.'\"',
+                'href=\"'.$jsonEscapedHomeUrl.'\"',
+                'href=\"'.$jsonEscapedHomeUrl.'\"',
+                'href=\"'.$jsonEscapedHomeUrl.'\"',
+                'href=\"'.$jsonEscapedHomeUrl.'\"',
+                'href=\"'.$jsonEscapedHomeUrl.'\"',
+                'href=\"'.$jsonEscapedHomeUrl.'\"',
+                'href=\"'.$jsonEscapedHomeUrl.'\"',
+                'href=\"'.$homeUrl.'\"',
+                'src=\"'.$jsonEscapedIngressPath.'\/livewire\/',
+                'src=\"'.$ingressPath.'/livewire/',
+                'src=\"'.$jsonEscapedIngressPath.'\/storage\/',
+                'src=\"'.$ingressPath.'/storage/',
+                'data-update-uri=\"'.$jsonEscapedIngressPath.'\/livewire\/',
+                'data-update-uri=\"'.$ingressPath.'/livewire/',
+            ],
+            $content
+        );
+
+        if ($rewritten !== $content) {
+            $response->setContent($rewritten);
+            $response->headers->remove('Content-Length');
+        }
+    }
+
+    protected function buildLogContext(Request $request, array $context): array
+    {
+        return [
+            'method' => $request->getMethod(),
+            'path' => '/'.ltrim($request->path(), '/'),
+            'route_name' => $request->route()?->getName(),
+            'host' => $context['host'],
+            'scheme' => $context['scheme'],
+            'ingress_path' => $context['ingress_path'],
+            'base_url' => $context['base_url'],
+            'livewire_asset_url' => config('livewire.asset_url'),
+            'users_exist' => User::exists(),
+            'authenticated' => $request->user() !== null,
+            'has_livewire_header' => $request->headers->has('X-Livewire'),
+            'session_id' => $request->hasSession() ? $request->session()->getId() : null,
+            'session_cookie_path' => config('session.path'),
+            'session_same_site' => config('session.same_site'),
+            'session_partitioned' => config('session.partitioned'),
+            'intended_url' => $request->hasSession() ? $request->session()->get('url.intended') : null,
+        ];
+    }
+}
diff --git a/app/Http/Responses/Auth/LoginResponse.php b/app/Http/Responses/Auth/LoginResponse.php
new file mode 100644
index 0000000..4779db5
--- /dev/null
+++ b/app/Http/Responses/Auth/LoginResponse.php
@@ -0,0 +1,28 @@
+<?php
+
+namespace App\Http\Responses\Auth;
+
+use App\Filament\Pages\Dashboard;
+use Filament\Auth\Http\Responses\LoginResponse as BaseLoginResponse;
+use Illuminate\Http\RedirectResponse;
+use Illuminate\Support\Facades\Log;
+use Livewire\Features\SupportRedirects\Redirector;
+
+class LoginResponse extends BaseLoginResponse
+{
+    public function toResponse($request): RedirectResponse|Redirector
+    {
+        $target = Dashboard::getUrl(panel: 'admin');
+
+        Log::info('Discount Bandit auth redirect', [
+            'page' => 'login_response',
+            'reason' => 'login_success_response',
+            'target' => $target,
+            'authenticated' => auth()->check(),
+            'session_id' => $request->hasSession() ? $request->session()->getId() : null,
+            'intended_url' => $request->hasSession() ? $request->session()->get('url.intended') : null,
+        ]);
+
+        return redirect()->intended($target);
+    }
+}
diff --git a/app/Providers/AppServiceProvider.php b/app/Providers/AppServiceProvider.php
index e788a8c..1e3286a 100644
--- a/app/Providers/AppServiceProvider.php
+++ b/app/Providers/AppServiceProvider.php
@@ -2,25 +2,40 @@

 namespace App\Providers;

+use App\Http\Responses\Auth\LoginResponse as IngressLoginResponse;
+use Filament\Auth\Http\Responses\Contracts\LoginResponse as LoginResponseContract;
 use Filament\Support\Facades\FilamentView;
 use Filament\Tables\Enums\FiltersLayout;
 use Filament\Tables\Enums\PaginationMode;
 use Filament\Tables\Table;
 use Filament\View\PanelsRenderHook;
+use Illuminate\Support\Facades\Route;
 use Illuminate\Support\HtmlString;
 use Illuminate\Support\ServiceProvider;
+use Livewire\Livewire;

 class AppServiceProvider extends ServiceProvider
 {
     /**
      * Register any application services.
      */
-    public function register(): void {}
+    public function register(): void
+    {
+        $this->app->bind(LoginResponseContract::class, IngressLoginResponse::class);
+    }

     /**
      * Bootstrap any application services.
      */
     public function boot(): void {
+        Livewire::setScriptRoute(function ($handle) {
+            return Route::get('/livewire/livewire.js', $handle);
+        });
+
+        Livewire::setUpdateRoute(function ($handle) {
+            return Route::post('/livewire/update', $handle)
+                ->middleware('web');
+        });

         Table::configureUsing(function (Table $table): void {
             $table->filtersLayout(FiltersLayout::AboveContentCollapsible)
diff --git a/app/Providers/Filament/AdminPanelProvider.php b/app/Providers/Filament/AdminPanelProvider.php
index 501de36..5fd06fe 100644
--- a/app/Providers/Filament/AdminPanelProvider.php
+++ b/app/Providers/Filament/AdminPanelProvider.php
@@ -47,6 +47,7 @@ class AdminPanelProvider extends PanelProvider
             ->default()
             ->id('admin')
             ->path('')
+            ->homeUrl(fn (): string => \App\Filament\Pages\Dashboard::getUrl(panel: 'admin'))
             ->login()
             ->registration(Register::class)
             ->login(action: Login::class)
diff --git a/bootstrap/app.php b/bootstrap/app.php
index 238feb0..898e196 100644
--- a/bootstrap/app.php
+++ b/bootstrap/app.php
@@ -13,6 +13,7 @@ return Application::configure(basePath: dirname(__DIR__))
     )
     ->withMiddleware(function (Middleware $middleware) {
         $middleware->trustProxies(at: '*');
+        $middleware->append(\App\Http\Middleware\TrustIngressPath::class);
     })
     ->withExceptions(function (Exceptions $exceptions) {
 //        $exceptions->render(function (Exception $e) {
diff --git a/docker/base_supervisord.conf b/docker/base_supervisord.conf
index b7efe54..0f354a4 100644
--- a/docker/base_supervisord.conf
+++ b/docker/base_supervisord.conf
@@ -2,6 +2,8 @@
 user=root
 nodaemon=true
 logfile=/logs/supervisord_stdout.log
+logfile_maxbytes=5MB
+logfile_backups=2
 pidfile=/var/run/supervisord.pid

 [supervisorctl]
@@ -14,7 +16,7 @@ supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface


 [program:octane]
-command=php artisan octane:frankenphp --host=$FRANKEN_HOST --port=80   --workers=4 --max-requests=1000 --admin-port=2019
+command=php artisan octane:frankenphp --host=$FRANKEN_HOST --port=80 --workers=2 --max-requests=500 --admin-port=2019
 user=root
 autostart=true
 autorestart=true
@@ -23,6 +25,10 @@ stdout_events_enabled=true
 stderr_events_enabled=true
 stdout_logfile=/logs/octane_stdout.log
 stderr_logfile=/logs/octane_stderr.log
+stdout_logfile_maxbytes=5MB
+stdout_logfile_backups=2
+stderr_logfile_maxbytes=5MB
+stderr_logfile_backups=2


 [program:scheduler]
@@ -34,16 +40,20 @@ autorestart=true
 numprocs=1
 redirect_stderr=true
 stdout_logfile=/logs/scheduler.log
+stdout_logfile_maxbytes=5MB
+stdout_logfile_backups=2

 [program:default_queue]
 process_name=%(program_name)s_%(process_num)02d
 user=root
-command=php artisan queue:work  --max-time=300 --sleep=1 --tries=1
+command=php artisan queue:work --max-time=180 --max-jobs=25 --memory=192 --timeout=60 --sleep=2 --tries=1
 autostart=true
 autorestart=true
 stopasgroup=true
 killasgroup=true
 numprocs=1
 redirect_stderr=true
-stdout_logfile=/var/log/default_worker.log
+stdout_logfile=/logs/default_worker.log
+stdout_logfile_maxbytes=5MB
+stdout_logfile_backups=2
 stopwaitsecs=300
diff --git a/routes/console.php b/routes/console.php
index 8a97a07..47d4af8 100644
--- a/routes/console.php
+++ b/routes/console.php
@@ -15,16 +15,21 @@ Schedule::command('discount:delete-links-out-of-stock')

 Schedule::call(function () {
     try {
+        $maxLinksPerStore = max(1, min(60, (int) env('DISCOUNT_BANDIT_MAX_LINKS_PER_STORE', 10)));
+        $queuedLinks = 0;
+
         // clear all jobs so products won't overlap in case they didn't finish on time.
         DB::table('jobs')->truncate();

-        Log::info("Products Schedule Started");
+        Log::info('Products Schedule Started', [
+            'max_links_per_store' => $maxLinksPerStore,
+        ]);

-        $stores = Store::withWhereHas('links', function ($query) {
+        $stores = Store::withWhereHas('links', function ($query) use ($maxLinksPerStore) {
             $query->withoutGlobalScopes()
                 ->orderBy("updated_at")
                 ->distinct(['key', 'store_id'])
-                ->limit(60);
+                ->limit($maxLinksPerStore);
         })
             ->where('status', StoreStatusEnum::Active)
             ->get();
@@ -34,14 +39,20 @@ Schedule::call(function () {
                 CrawlProductJob::dispatch($link->id)
                     ->onQueue($store->slug)
                     ->delay(now()->addSeconds($index * 5));
+                $queuedLinks++;
             }
         }

-        Log::info("Products Schedule Finished Successfully");
+        Log::info('Products Schedule Finished Successfully', [
+            'active_stores' => $stores->count(),
+            'queued_links' => $queuedLinks,
+        ]);
     } catch (Exception $e) {
         Log::error("Couldn't Run the Product Schedule, Error: \n".$e);
     }
-})->everyTenSeconds();
+})->name('discount-products-crawl')
+    ->cron(env('CRON', '0 */2 * * *'))
+    ->withoutOverlapping();

 // Schedule::job(\App\Jobs\CheckGroupPriceJob::class)->cron(config('settings.group_cron'));
